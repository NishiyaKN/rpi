services:
  price-tracker:
    image: price-tracker
    build: .
    container_name: yzk
    volumes:
      - ./src:/app/src
      - ./dc:/app/dc
      - ./requirements.txt:/app/requirements.txt
      - ./cronjob:/etc/cron.d/price-tracker
    command: cron -f
